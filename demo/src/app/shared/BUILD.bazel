package(default_visibility = ["//visibility:public"])

load("@angular//:index.bzl", "ng_module")

ng_module(
    name = "shared",
    srcs = glob(["**/*.ts"]),
    assets = glob(["**/*.html"]),
    tsconfig = "//:tsconfig.json",
    deps = [
        "//src",
        "@rxjs",
    ]
)
